(defn-file [m key val]
  (when (map? m)
    (setv index (find (second m) key))
    (if (>= 0 index)
      (set (third m) [index val])
      (do (pushBack (second m) key)
          (pushBack (third m) val)))
    m))
