(defn-file [map key val]
  (when (map? map)
    (setv index (find (keys m) k))
    (if (>= 0 index)
      (set (vals map) [index val])
      (do (pushBack (keys map) key)
          (pushBack (vals map) val)))
    map))
