(defn-file [m key val]
  (when (map? m)
    (setv index (find (keys m) k))
    (if (>= 0 index)
      (set (vals m) [index val])
      (do (pushBack (keys m) key)
          (pushBack (vals m) val)))
    m))
