(defn-file [obj]
  (cond
    (set? obj) (format ["#{%1}" (joinString (map sqisp-repr obj) ", ")])
    (map? obj) (format ["{%1}"
                        (joinString
                         (map (fn [x]
                                (joinString [(sqisp-repr (first x))
                                             (sqisp-repr (second x))]
                                            " "))
                              obj) ", ")])
    (vec? obj) (format ["[%1]" (joinString (map sqisp-repr obj) ", ")])
    (string? obj) obj
    true (str obj)))
